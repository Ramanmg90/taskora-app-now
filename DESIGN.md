# تسکورا · دیزاین‌سیستم «فلز مایع نئونی» (v2)

## چه چیزی در این نسخه عوض شد

### ۱) بکگراند متالیک‌تر
`lib/widgets/neon/neon_background.dart` یک لایه‌ی جدید گرفت:
- پایه‌ی گان‌متال تیره‌تر (بجای بنفش تخت)
- **برس فلزی مورب** (`_BrushedMetalPainter`) — خطوط نازک نامنظم، شبیه آلومینیوم برس‌خورده
- **نوار نور شناور** (`_SheenPainter`) — هر ۷ ثانیه یک انعکاس نور مورب از گوشه‌ای به گوشه‌ی دیگر می‌گذرد
- وینیت لبه‌های تیره‌تر برای عمق بیشتر
- شفق‌های نئونی رنگی قبلی هنوز هستند، فقط کم‌رنگ‌تر شدند تا بافت فلزی دیده شود

### ۲) لبه‌ی بِول فلزی روی همه‌ی کارت‌ها
`MetalBevelPainter` (در `glass.dart`) یک قاب گرادیانی روشن→تیره دور هر
`GlassCard`، `GlassIconButton` و `NeonNavBar` می‌کشد — دقیقاً مثل لبه‌ی
یک پنل فلزی صیقلی که از یک زاویه نور می‌گیرد. چون `GlassAppBar` و همه‌ی
صفحه‌ها از `GlassCard` استفاده می‌کنند، این افکت خودکار همه‌جا اعمال شد.
دکمه‌ها (`NeonButton`) هم یک جلای شیشه‌ای بالای خودشان گرفتند.

### ۳) تایمر تمرکز (جدید)
تب پنجم نوار ناوبری: **تایمر**. `lib/screens/timer/focus_timer_screen.dart`
- حلقه‌ی شمارش‌معکوس نئونی (`NeonCountdownRing`) با پالس نوری هنگام اجرا
- سه پیش‌فرض (پومودورو ۲۵/۵، عمیق ۵۰/۱۰، کوتاه ۱۵/۵) + استپر دستی
- چرخه‌ی خودکار کار↔استراحت با اعلان فوری در پایان هر مرحله
  (`NotificationService.showInstant`، کانال Android جدید `taskora_timer`)
- امکان وصل‌کردن تایمر به یکی از وظایف امروز

### ۴) رفع باگ واقعی: کرش هنگام حذف با سوایپ
در لیست وظایف، `Dismissible.onDismissed` قبلاً مستقیماً
`TaskProvider.deleteTask()` را صدا می‌زد — که خودش چند مرحله‌ی async
دارد (لغو یادآوری + حذف از DB + بارگذاری کامل مجدد). اگر رابط کاربری
قبل از پایان این زنجیره دوباره رندر می‌شد، Flutter خطای
**«A dismissed Dismissible widget is still part of the tree»** می‌داد.
راه‌حل: حذف خوش‌بینانه — آیدی وظیفه بلافاصله و هم‌زمان (نه async) به یک
`Set<int> _pendingDeleteIds` اضافه می‌شود و از لیست فیلتر می‌شود، بدون
معطلی برای پاسخ دیتابیس (`task_list_screen.dart`).

جاهای دیگر هم بازبینی شدند (نوتیفیکیشن، تم سوییچ/چک‌باکس، پارس رنگ‌ها،
منطق هفته‌ی جلالی) و باگ اضافه‌ای پیدا نشد.

### ۵) فونت — این‌بار واقعی
قبلاً فقط یک راهنمای کامنت‌شده در pubspec بود. حالا فونت **وزیرمتن**
(گرد، مینیمال، پرکاربردترین فونت مدرن فارسی) واقعاً از مخزن رسمی
دانلود و در `assets/fonts/` قرار گرفت (۸ وزن، از Thin تا Black، مجوز
OFL — رایگان برای استفاده‌ی تجاری). `pubspec.yaml` دیگر کامنت نیست.
علاوه‌براین همه‌ی شعاع‌های گرد (`AppRadius`) کمی بزرگ‌تر شدند
(مثلاً کارت‌ها از ۲۰ به ۲۴، دکمه‌ها از ۱۴ به ۱۸) تا حس گردتر و
مینیمال‌تری بدهند.

### ۶) فیلدهای ورودی — کامپوننت تازه
`lib/widgets/neon/neon_field.dart` → `NeonField`، جایگزین همه‌ی
`TextFormField`/`TextField`های فرم‌دار در اپ (ورود، ثبت‌نام، پروفایل،
فرم وظیفه، افزودن دسته‌بندی، تغییر رمز):
- لیبل شناور با انیمیشن
- حلقه‌ی نوری که با فوکوس شدت می‌گیرد (رنگ هر فیلد قابل تنظیم است)
- آیکون که با فوکوس رنگ و مقیاس عوض می‌کند
- **لرزش کوتاه و میراشونده** وقتی خطای اعتبارسنجی تازه ظاهر می‌شود
- کاملاً با `Form.validate()` بیرونی سازگار است (چون خودش `FormField<String>` است)

نوار جست‌وجوی صفحه‌ی وظایف هم یک گلوی نئونی واکنش‌گرا به فوکوس گرفت.

## اجرا

```bash
flutter pub get
flutter run
```

اگر خطایی از `flutter analyze` گرفتی، متنش را بفرست.
